"""
ATENÇÃO: Este arquivo FastAPI é legado.

O servidor principal do projeto usa Flask (app.py) e blueprints
em projeto/app/routes. Para evitar duplicidade de frameworks,
este módulo não deve ser usado na execução padrão.

Se quiser migrar tudo para FastAPI, crie um plano de conversão
e unifique as rotas existentes em routers FastAPI.
"""

# Arquivo legado — sem código executável para evitar conflitos.